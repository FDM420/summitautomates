# Firebase App Hosting configuration
# https://firebase.google.com/docs/app-hosting/configure
#
# Build/runtime environment variables for the Next.js app.
# NEXT_PUBLIC_* vars are inlined into the client bundle at BUILD time.

env:
  - variable: NEXT_PUBLIC_CALENDAR_URL
    value: https://calendly.com/admin-fdm-summit-systems/30min
    availability:
      - BUILD
      - RUNTIME

  # --- WhatsApp Cloud API webhook ---
  # Verify token: shared string Meta echoes back during webhook verification.
  # Low-sensitivity, kept inline so verification works without extra setup.
  - variable: WHATSAPP_VERIFY_TOKEN
    value: summit_wa_0f662d5280c3be8a3661b64d32fbbe2c38f638b0
    availability:
      - RUNTIME

  # Sensitive values live in Cloud Secret Manager. Create each secret FIRST:
  #   firebase apphosting:secrets:set whatsapp-app-secret
  #   firebase apphosting:secrets:set whatsapp-access-token
  #   firebase apphosting:secrets:set whatsapp-phone-number-id
  #   firebase apphosting:secrets:set openai-api-key   # add later for AI replies
  # THEN uncomment the matching blocks below and redeploy. (Referencing a secret
  # that does not exist yet fails the App Hosting build, so keep these commented
  # until the secret is created.)
  #
  # - variable: WHATSAPP_APP_SECRET
  #   secret: whatsapp-app-secret
  #   availability:
  #     - RUNTIME
  # - variable: WHATSAPP_ACCESS_TOKEN
  #   secret: whatsapp-access-token
  #   availability:
  #     - RUNTIME
  # - variable: WHATSAPP_PHONE_NUMBER_ID
  #   secret: whatsapp-phone-number-id
  #   availability:
  #     - RUNTIME
  # - variable: OPENAI_API_KEY
  #   secret: openai-api-key
  #   availability:
  #     - RUNTIME
